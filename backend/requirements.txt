# ============================================================================
# Parkinson Detection Backend - Complete Requirements
# ============================================================================
# This file contains ALL dependencies needed to run the backend flawlessly
# Install with: pip install -r requirements.txt
# ============================================================================

# Web Framework
Flask==2.3.3
Flask-CORS==4.0.0

# Core Scientific Computing
numpy>=1.26.4,<2.4.0
pandas>=2.0.3
scipy>=1.11.3

# Machine Learning
scikit-learn>=1.3.0
xgboost>=1.7.6

# Audio Processing
librosa==0.10.1
soundfile>=0.12.1          # Required by librosa for audio I/O
audioread>=3.0.0           # Audio file reading support
resampy>=0.4.2             # Audio resampling for librosa

# Audio Format Conversion
pydub>=0.25.1              # WebM to WAV conversion
# Note: pydub requires ffmpeg to be installed on system

# Fast Compilation (Optional - librosa works fine without it)
# Removed numba because it doesn't support Python 3.14 yet
# llvmlite is a dependency of numba, so also removed

# Utilities
joblib>=1.3.2              # Model serialization
click>=8.1.0               # CLI utilities (used for colored output suppression)
requests>=2.31.0           # HTTP client (for testing scripts)

# Excel Export (if needed in future)
openpyxl>=3.1.2            # Excel file support for pandas

# Color output support (optional but recommended)
colorama>=0.4.6            # Cross-platform colored terminal output

# ============================================================================
# Python Version Requirement: >= 3.10, < 3.14
# ============================================================================
# Recommended: Python 3.13.5 (latest stable version fully supported)
# Note: Python 3.14 is not yet fully supported by numba and other packages
# Tested with Python 3.10, 3.11, 3.12, 3.13 on Windows and Linux
# ============================================================================