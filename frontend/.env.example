# Environment Variables for Vercel Deployment

# Backend API URL - ngrok custom domain for the Flask backend
BACKEND_URL=https://ostensible-unvibrant-clarisa.ngrok-free.dev

# Note: 
# - Do NOT include trailing slash
# - Do NOT include /api at the end (config.js automatically adds it)
# - The frontend will automatically append /api to create full URL like:
#   https://ostensible-unvibrant-clarisa.ngrok-free.dev/api/health
